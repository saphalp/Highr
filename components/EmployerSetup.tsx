import { View, Text } from 'react-native'
import React from 'react'
import {
  TextInput,
  Button,
  Avatar,
  HelperText,
  Divider,
  Surface,
} from "react-native-paper";

export default function EmployerSetup() {
  return (
    <View>
      <Text>EmployerSetup</Text>
    </View>
  )
}